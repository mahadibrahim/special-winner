---
import '../../../../styles/globals.css';
import Navigation from '../../../../components/navigation';
import Footer from '../../../../components/footer';
import DevelopmentReport from '../../../../components/dashboard/development-report';
import AchievementsDisplay from '../../../../components/dashboard/achievements-display';

// Get user from middleware locals
const user = Astro.locals.user;

// Redirect if not logged in
if (!user) {
  return Astro.redirect('/signin?redirect=/dashboard');
}

// Get child ID from params
const { id } = Astro.params;

if (!id) {
  return Astro.redirect('/dashboard');
}
---

<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Development Progress - Aspire Sports</title>
    <style>
      /* Page load animation */
      @keyframes fadeSlideIn {
        from {
          opacity: 0;
          transform: translateY(12px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .page-content {
        animation: fadeSlideIn 0.5s ease-out forwards;
      }

      /* Background pattern */
      .development-bg {
        background-image:
          radial-gradient(ellipse 80% 50% at 50% -20%, rgba(204, 68, 44, 0.08), transparent),
          radial-gradient(circle at 80% 80%, rgba(59, 130, 246, 0.04), transparent);
      }
    </style>
  </head>
  <body class="min-h-screen flex flex-col bg-[#0a0a0f] text-white">
    <Navigation client:load />

    <main class="flex-1 pt-24 pb-16 px-4 development-bg">
      <div class="max-w-7xl mx-auto page-content">
        <!-- Breadcrumb -->
        <nav class="flex items-center gap-2 text-sm text-gray-500 mb-6">
          <a href="/dashboard" class="hover:text-white transition-colors">Dashboard</a>
          <span>/</span>
          <a href={`/dashboard/children/${id}`} class="hover:text-white transition-colors">Child Profile</a>
          <span>/</span>
          <span class="text-white">Development Progress</span>
        </nav>

        <!-- Header -->
        <header class="mb-8">
          <div class="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4">
            <div>
              <h1 class="text-2xl sm:text-3xl font-bold text-white mb-2">
                Development Progress
              </h1>
              <p class="text-gray-500">
                Track skill development across technical, tactical, physical, and psychological domains.
              </p>
            </div>

            <div class="flex gap-2">
              <a href={`/dashboard/children/${id}`} class="inline-flex items-center justify-center px-4 py-2 text-sm font-medium rounded-lg border border-white/10 text-gray-400 hover:text-white hover:bg-white/5 transition-colors">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                </svg>
                Back to Profile
              </a>
            </div>
          </div>
        </header>

        <!-- Main Content -->
        <div class="grid gap-8 lg:grid-cols-3">
          <!-- Left Column - Development Report -->
          <div class="lg:col-span-2">
            <DevelopmentReport client:load familyMemberId={id} />
          </div>

          <!-- Right Column - Achievements -->
          <div class="space-y-6">
            <div class="p-6 rounded-2xl bg-white/[0.02] border border-white/[0.06]">
              <h2 class="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                <svg class="w-5 h-5 text-amber-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
                </svg>
                Achievements
              </h2>
              <AchievementsDisplay client:load familyMemberId={id} compact limit={5} showLocked={false} />
            </div>

            <!-- Quick Tips -->
            <div class="p-6 rounded-2xl bg-gradient-to-br from-primary/10 to-orange-500/10 border border-primary/20">
              <h3 class="font-semibold text-white mb-3">Understanding Levels</h3>
              <div class="space-y-2 text-sm">
                <div class="flex items-center gap-2">
                  <span class="w-6 h-6 rounded-full bg-gray-500/20 flex items-center justify-center text-xs text-gray-400">1</span>
                  <span class="text-gray-400">Beginner - Just starting</span>
                </div>
                <div class="flex items-center gap-2">
                  <span class="w-6 h-6 rounded-full bg-blue-500/20 flex items-center justify-center text-xs text-blue-400">2</span>
                  <span class="text-gray-400">Developing - Building basics</span>
                </div>
                <div class="flex items-center gap-2">
                  <span class="w-6 h-6 rounded-full bg-emerald-500/20 flex items-center justify-center text-xs text-emerald-400">3</span>
                  <span class="text-gray-400">Competent - Solid foundation</span>
                </div>
                <div class="flex items-center gap-2">
                  <span class="w-6 h-6 rounded-full bg-amber-500/20 flex items-center justify-center text-xs text-amber-400">4</span>
                  <span class="text-gray-400">Proficient - Strong skills</span>
                </div>
                <div class="flex items-center gap-2">
                  <span class="w-6 h-6 rounded-full bg-primary/20 flex items-center justify-center text-xs text-primary">5</span>
                  <span class="text-gray-400">Advanced - Exceptional</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>

    <Footer client:load />
  </body>
</html>
