---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { AdminLayout } from "@/components/admin/admin-layout";
import { RevenueReport } from "@/components/admin/revenue-report";

export const prerender = false;

const user = Astro.locals.user!;
const primaryRole = (Astro.locals.userRoles ?? [])[0]?.name ?? "parent";
---

<BaseLayout title="Revenue — Reports — Admin — Aspire Sports" navigation={false} footer={false}>
  <AdminLayout
    client:load
    role={primaryRole}
    currentPath="/admin/reports/revenue"
    user={{ firstName: user.firstName, lastName: user.lastName, email: user.email }}
  >
    <RevenueReport client:load />
  </AdminLayout>
</BaseLayout>
