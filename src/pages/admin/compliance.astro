---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { AdminLayout } from '../../components/admin/admin-layout';
import { getPrimaryRoleName } from "@/lib/auth";
import { ComplianceList } from '../../components/admin/compliance-list';

// Auth check handled by middleware
const user = Astro.locals.user!;
const primaryRole = getPrimaryRoleName(Astro.locals.userRoles);
---

<BaseLayout title="Compliance — Admin — Aspire Sports" navigation={false} footer={false}>
  <AdminLayout
    client:load
    role={primaryRole}
    currentPath="/admin/compliance"
    user={{ firstName: user.firstName, lastName: user.lastName, email: user.email }}
  >
    <ComplianceList client:load />
  </AdminLayout>
</BaseLayout>
