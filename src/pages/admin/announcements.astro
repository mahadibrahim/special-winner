---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { AdminLayout } from '../../components/admin/admin-layout';
import { AnnouncementsList } from '../../components/admin/announcements-list';

// Middleware guarantees user is an admin for /admin routes.
const user = Astro.locals.user!;
---

<BaseLayout title="Announcements — Admin — Aspire Sports" navigation={false} footer={false}>
  <AdminLayout
    client:load
    currentPath="/admin/announcements"
    user={{ firstName: user.firstName, lastName: user.lastName, email: user.email }}
  >
    <AnnouncementsList client:load />
  </AdminLayout>
</BaseLayout>
