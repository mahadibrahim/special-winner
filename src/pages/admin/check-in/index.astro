---
// Check-in now lives inside the command center. 301 redirect preserves any
// in-the-wild bookmarks or stale links (redirects straight to /admin/venue
// rather than through the now-retired /admin/venue/check-in hop).
return Astro.redirect("/admin/venue", 301);
---
