---
// Walk-up registration is now a flow inside the command center rather than
// its own page — see src/components/admin/venue/command/WalkInFlow.tsx.
// 308 (permanent) redirect preserves any in-the-wild bookmarks/nav links.
return Astro.redirect("/admin/venue", 308);
---
