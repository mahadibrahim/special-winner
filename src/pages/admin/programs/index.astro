---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { AdminLayout } from '../../../components/admin/admin-layout';
import { ProgramsList } from '../../../components/admin/programs-list';

// Middleware guarantees user is an admin for /admin routes.
const user = Astro.locals.user!;
const primaryRole = (Astro.locals.userRoles ?? [])[0]?.roleName ?? "parent";
const isSuperAdmin = (Astro.locals.userRoles ?? []).some((r) => r.name === "super_admin");
---

<BaseLayout title="Programs — Admin — Aspire Sports" navigation={false} footer={false}>
  <AdminLayout
    client:load
    role={primaryRole}
    currentPath="/admin/programs"
    user={{ firstName: user.firstName, lastName: user.lastName, email: user.email }}
  >
    <ProgramsList client:load isSuperAdmin={isSuperAdmin} />
  </AdminLayout>
</BaseLayout>
