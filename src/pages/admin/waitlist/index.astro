---
import '../../../styles/globals.css';
import { AdminLayout } from '../../../components/admin/admin-layout';
import { WaitlistManager } from '../../../components/admin/waitlist-manager';

// Check if user is admin
const user = Astro.locals.user;
if (!user) {
  return Astro.redirect('/signin?returnUrl=/admin/waitlist');
}
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Waitlist Management - Admin - Aspire Sports</title>
  </head>
  <body>
    <AdminLayout
      client:load
      currentPath="/admin/waitlist"
      user={{ firstName: user.firstName, lastName: user.lastName, email: user.email }}
    >
      <WaitlistManager client:load />
    </AdminLayout>
  </body>
</html>
