---
// Legacy URL from before the password-removal migration. 301s straight to
// /signin (the canonical magic-link page) in a single hop so old
// email-template links and bookmarks still resolve. Authed users are
// bounced to /dashboard upstream by middleware's REDIRECT_IF_AUTHED.
return Astro.redirect("/signin", 301);
---
