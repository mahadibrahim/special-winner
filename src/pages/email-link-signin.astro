---
// /signin IS the magic-link form now (passwords removed from the UI),
// so this older URL just redirects to /signin to keep one canonical
// entry point. Old emails / bookmarks pointing here still work.
return Astro.redirect("/signin", 301);
---
