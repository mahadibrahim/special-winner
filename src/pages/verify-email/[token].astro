---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { consumeEmailVerificationToken } from '@/lib/auth/email-verification';

const { token } = Astro.params;

let verified = false;
let error: string | null = null;

if (token) {
  // Shared consumer hashes the token before lookup (storage holds the digest,
  // not the raw value) and refreshes sessions on success. See
  // src/lib/auth/email-verification.ts.
  const result = await consumeEmailVerificationToken(token);
  if (result.ok) {
    verified = true;
  } else {
    error = "This verification link is invalid or has expired.";
  }
} else {
  error = "Invalid verification link.";
}
---

<BaseLayout title={`${verified ? 'Email Verified' : 'Verification Failed'} — Aspire Sports`}>
  <main id="main-content" class="flex-1 flex items-center justify-center py-12 px-4 pt-24">
    <div class="w-full max-w-md">
      <!-- Card -->
      <div class="bg-cream-2 border border-border rounded-2xl p-8">
        {verified ? (
          <div class="text-center">
            <div class="w-16 h-16 bg-green-500/10 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
            </div>
            <h1 class="text-2xl font-bold text-ink mb-2">Email Verified!</h1>
            <p class="text-ink-muted mb-6">
              Your email has been successfully verified. You can now access all features of your account.
            </p>
            <a
              href="/dashboard"
              class="inline-flex items-center justify-center rounded-md text-sm font-medium transition-colors bg-primary text-primary-foreground hover:bg-primary/90 h-11 px-8"
            >
              Go to Dashboard
            </a>
          </div>
        ) : (
          <div class="text-center">
            <div class="w-16 h-16 bg-destructive/10 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-destructive" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
              </svg>
            </div>
            <h1 class="text-2xl font-bold text-ink mb-2">Verification Failed</h1>
            <p class="text-ink-muted mb-6">
              {error}
            </p>
            <div class="space-y-3">
              <a
                href="/dashboard"
                class="inline-flex items-center justify-center rounded-md text-sm font-medium transition-colors bg-primary text-primary-foreground hover:bg-primary/90 h-11 px-8 w-full"
              >
                Go to Dashboard
              </a>
              <p class="text-ink-faint text-sm">
                You can request a new verification email from your dashboard settings.
              </p>
            </div>
          </div>
        )}
      </div>
    </div>
  </main>
</BaseLayout>
