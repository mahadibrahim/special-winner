# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# Bitwarden Secrets Manager — local plaintext secret load file (never commit).
# The .example template IS tracked; the filled-in copy is not.
secrets.local.csv

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Local Netlify folder
.netlify

# Playwright
playwright-report/
test-results/

# Superpowers brainstorming
.superpowers/

# Git worktrees (isolated workspaces for parallel development)
.worktrees/

# Claude Code local state (skills, settings, lock files)
.claude/scheduled_tasks.lock
.claude/settings.json
.claude/skills/
.claude/worktrees/

# Local agent / skill tooling
.agents/
skills-lock.json

# Netlify CLI local cache (functions emulator etc.). User-authored
# scheduled functions live at netlify/functions/ and are tracked.
netlify/edge-functions-import-map.json
netlify/state.json
netlify/cache/
netlify/build/
netlify/blobs-serve/

# Generated PDFs (minibooks, KDP book interiors, strategy docs) — regenerate
# via scripts/generate-minibook-pdfs.ts; reviewers re-render rather than diff binaries.
pdfs/

# Email render preview output (C11 verification pass)
dist-preview/
